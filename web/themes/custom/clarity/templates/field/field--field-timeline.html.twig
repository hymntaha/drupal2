<section id="cd-timeline">
    {% for item in items %}
        <div class="cd-timeline-block">
            <div class="cd-timeline-img red-dot"></div>
            {{ item.content }}
        </div>
    {% endfor %}
</section>